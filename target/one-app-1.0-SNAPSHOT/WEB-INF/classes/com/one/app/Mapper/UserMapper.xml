<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.one.app.Mapper.UserMapper">
    <select id="getUserById" resultType="com.one.app.Model.User">
      select * from users where userId = #{userId}
     </select>
    <select id="getUsers" resultType="com.one.app.Model.User">
      select * from users
     </select>
    <insert id="addUser" parameterType="com.one.app.Model.User">
      insert into users(userName,gender,age) values (#{userName},#{gender},#{age})
    </insert>
    <update id="updateUser" parameterType="com.one.app.Model.User">
        update users set userName=${userName},gender = #{gender}, age = #{age} where userId = #{userId}
    </update>
    <delete id="deleteUserById" parameterType="int">
        delete from users where userId=#{userId}
    </delete>
</mapper>